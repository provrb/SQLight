#pragma once

// WX
#include <wx/colour.h> // wxColour

namespace AppColours {
    namespace SQLSyntax {
        const wxColour IDENTIFIER = wxColour(166, 103, 168);
        const wxColour NUMBER = wxColour(191, 123, 44);
        const wxColour WORD = wxColour(47, 63, 138);
        const wxColour COMMENT = wxColour(70, 133, 73);
        const wxColour COMMENTLINE = wxColour(70, 133, 73);
        const wxColour STRING = wxColour(161, 84, 84);
    }
}
